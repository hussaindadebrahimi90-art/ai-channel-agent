# GitHub Actions workflow
name: AI Channel Agent

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Run Agent
        run: python -c "print('AI Channel Agent is running!')"
